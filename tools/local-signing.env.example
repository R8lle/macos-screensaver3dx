# Copy to tools/local-signing.env (gitignored) and fill in your values.
# Required for ./tools/release.sh (Developer ID + notarization).

TEAM_ID=YOUR_RELEASE_TEAM_ID
BUNDLE_ID=de.r8lle.screensaver.matrix3dx
SIGN_ID="Developer ID Application"
PROFILE=notary-matrix3dx
